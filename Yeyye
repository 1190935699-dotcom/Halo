local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Guest 666 Hub | V1.4 TYCOON UPDATE",
   LoadingTitle = "Guest 666 Hub | All in One Special",
   LoadingSubtitle = "By Guest666_20 (Tiktok)",
   ConfigurationSaving = { Enabled = true, FolderName = "Guest666_V14_Tycoon", FileName = "Main" },
   Discord = { Enabled = false },
   KeySystem = false,
})

-- ==========================================
-- [ระบบตัวแปรพื้นฐาน]
-- ==========================================
local SavedPos, WalkSpeedValue = nil, 16
local ESP_Enabled, Object_ESP_Enabled, InfiniteJumpEnabled = false, false, false
local NPCAimEnabled, InstantProximityEnabled, FullBrightEnabled = false, false, false
local SelectedObject = "None"

-- FOV Circle สีฟ้าเฉพาะกรอบ
local FOV_Circle = Drawing.new("Circle")
FOV_Circle.Visible = false
FOV_Circle.Thickness = 2
FOV_Circle.Color = Color3.fromRGB(0, 162, 255)
FOV_Circle.Filled = false
FOV_Circle.Radius = 100
FOV_Circle.Transparency = 0.7

-- ==========================================
-- [Nuvex Menu ตัวแปร]
-- ==========================================
local NoclipEnabled = false
local flying = false
local flySpeed = 1
local bodyVelocity, bodyGyro
local TPWalkEnabled = false
local TPWalkDistance = 5
local InvisibleEnabled = false
local GodModeEnabled = false
local HitboxEnabled = false

-- ==========================================
-- [Functions]
-- ==========================================
local function Noclip()
    if NoclipEnabled then
        local char = game.Players.LocalPlayer.Character
        if char then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end
end

local function Fly()
    local char = game.Players.LocalPlayer.Character
    local humanoid = char and char:FindFirstChild("Humanoid")
    if not humanoid then return end
    
    if flying then
        flying = false
        if bodyVelocity then bodyVelocity:Destroy() end
        if bodyGyro then bodyGyro:Destroy() end
        humanoid.PlatformStand = false
    else
        flying = true
        bodyVelocity = Instance.new("BodyVelocity")
        bodyGyro = Instance.new("BodyGyro")
        bodyVelocity.MaxForce = Vector3.new(1, 1, 1) * 100000
        bodyGyro.MaxTorque = Vector3.new(1, 1, 1) * 100000
        bodyVelocity.Parent = char.HumanoidRootPart
        bodyGyro.Parent = char.HumanoidRootPart
        humanoid.PlatformStand = true
        
        game:GetService("RunService").RenderStepped:Connect(function()
            if flying then
                local moveDir = Vector3.new()
                local cam = workspace.CurrentCamera
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then
                    moveDir = moveDir + cam.CFrame.LookVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then
                    moveDir = moveDir - cam.CFrame.LookVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then
                    moveDir = moveDir + cam.CFrame.RightVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then
                    moveDir = moveDir - cam.CFrame.RightVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.Space) then
                    moveDir = moveDir + Vector3.new(0, 1, 0)
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.LeftControl) then
                    moveDir = moveDir - Vector3.new(0, 1, 0)
                end
                bodyVelocity.Velocity = moveDir * flySpeed
                bodyGyro.CFrame = cam.CFrame
            end
        end)
    end
end

local function TPWalk()
    if TPWalkEnabled then
        local char = game.Players.LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        local humanoid = char and char:FindFirstChild("Humanoid")
        if hrp and humanoid and humanoid.MoveDirection.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + hrp.CFrame.LookVector * TPWalkDistance
        end
    end
end

local function ToggleInvisible()
    local char = game.Players.LocalPlayer.Character
    if InvisibleEnabled and char then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Transparency = 1
            end
        end
    elseif char then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Transparency = 0
            end
        end
    end
end

local function ToggleGodMode()
    local char = game.Players.LocalPlayer.Character
    local humanoid = char and char:FindFirstChild("Humanoid")
    if GodModeEnabled and humanoid then
        humanoid.MaxHealth = math.huge
        humanoid.Health = math.huge
        humanoid.BreakJointsOnDeath = false
    elseif humanoid then
        humanoid.MaxHealth = 100
        humanoid.BreakJointsOnDeath = true
    end
end

local function ToggleHitbox()
    for _, v in pairs(game.Players:GetPlayers()) do
        if v ~= game.Players.LocalPlayer then
            local targetChar = v.Character
            if targetChar and targetChar:FindFirstChild("HumanoidRootPart") then
                targetChar.HumanoidRootPart.Size = HitboxEnabled and Vector3.new(8, 8, 8) or Vector3.new(2, 2, 1)
            end
        end
    end
end

-- ==========================================
-- [Instant Proximity & Infinite Jump]
-- ==========================================
game:GetService("ProximityPromptService").PromptButtonHoldBegan:Connect(function(p)
    if InstantProximityEnabled then fireproximityprompt(p) end
end)

game:GetService("UserInputService").JumpRequest:Connect(function()
    if InfiniteJumpEnabled then
        local char = game.Players.LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") then
            char.Humanoid:ChangeState("Jumping")
        end
    end
end)

-- ==========================================
-- [Main Render Loop]
-- ==========================================
game:GetService("RunService").RenderStepped:Connect(function()
    FOV_Circle.Position = Vector2.new(workspace.CurrentCamera.ViewportSize.X / 2, workspace.CurrentCamera.ViewportSize.Y / 2)
    
    if NoclipEnabled then pcall(Noclip) end
    if TPWalkEnabled then pcall(TPWalk) end
    if InvisibleEnabled then pcall(ToggleInvisible) end
    if GodModeEnabled then pcall(ToggleGodMode) end
    if HitboxEnabled then pcall(ToggleHitbox) else pcall(ToggleHitbox) end
    
    if FullBrightEnabled then
        game.Lighting.Brightness = 2
        game.Lighting.ClockTime = 14
        game.Lighting.FogEnd = 100000
    end
    
    if NPCAimEnabled then
        local target, dist = nil, FOV_Circle.Radius
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("Model") and v:FindFirstChild("Humanoid") and not game.Players:GetPlayerFromCharacter(v) then
                local head = v:FindFirstChild("Head")
                if head then
                    local screenPos, onScreen = workspace.CurrentCamera:WorldToViewportPoint(head.Position)
                    if onScreen then
                        local mag = (Vector2.new(screenPos.X, screenPos.Y) - FOV_Circle.Position).Magnitude
                        if mag < dist then dist, target = mag, head end
                    end
                end
            end
        end
        if target then workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, target.Position) end
    end
    
    pcall(function()
        if game.Players.LocalPlayer.Character then
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = WalkSpeedValue
        end
        
        if ESP_Enabled then
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and v:FindFirstChild("Humanoid") and not v:FindFirstChild("G666_ESP") then
                    local hl = Instance.new("Highlight", v)
                    hl.Name = "G666_ESP"
                    hl.FillColor = Color3.fromRGB(255, 0, 0)
                end
            end
        end
        
        if Object_ESP_Enabled and SelectedObject ~= "None" then
            for _, v in pairs(workspace:GetDescendants()) do
                if v.Name:lower():find(SelectedObject:lower()) and not v:FindFirstChild("Obj_HL") then
                    local hl = Instance.new("Highlight", v)
                    hl.Name = "Obj_HL"
                    hl.FillColor = Color3.fromRGB(255, 255, 0)
                end
            end
        end
    end)
end)

-- ==========================================
-- [TAB: VISUAL & FOV]
-- ==========================================
local VisualTab = Window:CreateTab("Visual & FOV", 4483362458)

VisualTab:CreateSection("ESP Highlight")
VisualTab:CreateToggle({ Name = "Enable ESP (ผู้เล่น/NPC)", CurrentValue = false, Callback = function(V) ESP_Enabled = V end })
VisualTab:CreateToggle({ Name = "Full Bright", CurrentValue = false, Callback = function(V) FullBrightEnabled = V end })
VisualTab:CreateButton({ Name = "Plasticine Vision", Callback = function()
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("BasePart") then
            v.Material = Enum.Material.Plastic
            v.Reflectance = 0
        end
    end
end })

VisualTab:CreateSection("Object ESP")
VisualTab:CreateDropdown({
    Name = "เลือกสิ่งของ",
    Options = {"None", "Coin", "Key", "Gun", "Knife", "Chest", "Book", "Lever", "Battery"},
    CurrentOption = "None",
    Callback = function(Option) SelectedObject = Option[1] end,
})
VisualTab:CreateToggle({ Name = "Enable Object ESP", CurrentValue = false, Callback = function(V) Object_ESP_Enabled = V end })

VisualTab:CreateSection("FOV Circle สีฟ้า")
VisualTab:CreateToggle({ Name = "แสดง FOV Circle", CurrentValue = false, Callback = function(V) FOV_Circle.Visible = V end })
VisualTab:CreateSlider({ Name = "ขนาด FOV", Range = {10, 500}, Increment = 1, CurrentValue = 100, Callback = function(V) FOV_Circle.Radius = V end })

-- ==========================================
-- [TAB: UTILITY]
-- ==========================================
local UtilTab = Window:CreateTab("Utility", 4483362458)

UtilTab:CreateSection("Position Control")
UtilTab:CreateButton({ Name = "เซฟตำแหน่ง", Callback = function()
    SavedPos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
end })
UtilTab:CreateButton({ Name = "เทเลพอร์ตไปตำแหน่งที่เซฟ", Callback = function()
    if SavedPos then game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = SavedPos end
end })

UtilTab:CreateSection("Movement")
UtilTab:CreateToggle({ Name = "Infinite Jump", CurrentValue = false, Callback = function(V) InfiniteJumpEnabled = V end })
UtilTab:CreateToggle({ Name = "Instant Proximity", CurrentValue = false, Callback = function(V) InstantProximityEnabled = V end })
UtilTab:CreateToggle({ Name = "NPC Aim Assist", CurrentValue = false, Callback = function(V) NPCAimEnabled = V end })
UtilTab:CreateSlider({ Name = "Walk Speed", Range = {16, 500}, Increment = 1, CurrentValue = 16, Callback = function(V) WalkSpeedValue = V end })

-- ==========================================
-- [TAB: NUVEX MENU]
-- ==========================================
local NuvexTab = Window:CreateTab("Nuvex Menu", 4483362458)

NuvexTab:CreateSection("หลัก")
NuvexTab:CreateToggle({ Name = "Noclip (ทะลุ)", CurrentValue = false, Callback = function(V) NoclipEnabled = V end })
NuvexTab:CreateToggle({ Name = "Fly (บิน)", CurrentValue = false, Callback = function(V) if V then Fly() else Fly() end end })
NuvexTab:CreateSlider({ Name = "ความเร็วบิน", Range = {1, 50}, Increment = 1, CurrentValue = 1, Callback = function(V) flySpeed = V end })
NuvexTab:CreateButton({ Name = "TP to Mouse", Callback = function()
    local mouse = game.Players.LocalPlayer:GetMouse()
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(mouse.Hit.p)
end })

NuvexTab:CreateSection("TP Walk")
NuvexTab:CreateToggle({ Name = "เปิด TP Walk", CurrentValue = false, Callback = function(V) TPWalkEnabled = V end })
NuvexTab:CreateSlider({ Name = "ระยะ TP Walk", Range = {1, 20}, Increment = 1, CurrentValue = 5, Callback = function(V) TPWalkDistance = V end })

NuvexTab:CreateSection("เครื่องมือ")
NuvexTab:CreateToggle({ Name = "Hitbox (ขยายโดน)", CurrentValue = false, Callback = function(V) HitboxEnabled = V end })
NuvexTab:CreateButton({ Name = "F3X (Build)", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/studiobuilds/F3X/main/F3X.lua"))()
end })
NuvexTab:CreateButton({ Name = "Infinite Yield", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end })

NuvexTab:CreateSection("แกล้ง")
NuvexTab:CreateButton({ Name = "Fling ตัวเอง", Callback = function()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(0, 5000, 0)
end })
NuvexTab:CreateButton({ Name = "แช่แข็งตัวเอง 3 วิ", Callback = function()
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        local oldSpeed = char.Humanoid.WalkSpeed
        char.Humanoid.WalkSpeed = 0
        task.wait(3)
        char.Humanoid.WalkSpeed = oldSpeed
    end
end })

NuvexTab:CreateSection("หายตัว / อมตะ")
NuvexTab:CreateToggle({ Name = "Invisible (มองไม่เห็น)", CurrentValue = false, Callback = function(V)
    InvisibleEnabled = V
    ToggleInvisible()
end })
NuvexTab:CreateToggle({ Name = "God Mode (อมตะ)", CurrentValue = false, Callback = function(V)
    GodModeEnabled = V
    ToggleGodMode()
end })

-- ==========================================
-- [TAB: GAME SCRIPTS]
-- ==========================================
local GameTab = Window:CreateTab("Game Scripts", 4483362458)

GameTab:CreateSection("MM2 Scripts")
GameTab:CreateButton({ Name = "SomTank Hub", Callback = function()
    loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/36aa7a52f557fa2d7e5dbedae04d267b.lua"))()
end })
GameTab:CreateButton({ Name = "Vision Hub", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/orialdev/VisionHub/refs/heads/main/main.lua"))()
end })
GameTab:CreateButton({ Name = "1220 Hub", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/1220-128bit/1220Hub/refs/heads/main/1220Hub.lua"))()
end })
GameTab:CreateButton({ Name = "Dias Hub", Callback = function()
    loadstring(game:HttpGet("https://github.com/DiasMm2Script/murder-Mystery-2/raw/refs/heads/main/murder%20Mystery%202"))()
end })
GameTab:CreateButton({ Name = "Overdrive Hub MM2", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/yxkal999YT/overdrivehub.xyz/refs/heads/main/auth.lua", true))()
end })

GameTab:CreateSection("Universal Scripts")
GameTab:CreateButton({ Name = "Overdrive Hub Universal", Callback = function()
    loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Overdrive-h-125904"))()
end })
GameTab:CreateButton({ Name = "YARHM HUB", Callback = function()
    loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-YARHM-12403"))()
end })
GameTab:CreateButton({ Name = "Zoooo Script", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/ZanoX-Community/ZanoX-Hub/main/Main.lua"))()
end })

GameTab:CreateSection("Other")
GameTab:CreateButton({ Name = "Wack Hub (คนไทย)", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/wackshopr-tech/script-roblox-all/refs/heads/main/Main-GUI-2.lua"))()
end })
GameTab:CreateButton({ Name = "DropKick Fling", Callback = function()
    loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fe-DropKick-Script-165813"))()
end })
GameTab:CreateButton({ Name = "FPS Boost", Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/FRAXXontop/Syrenixscriptz/refs/heads/main/SyrenixFPS.lua"))()
end })

Rayfield:Notify({ Title = "Guest 666 Hub V1.4", Content = "โหลดสำเร็จ! ฟังก์ชั่นครบทุกอย่าง", Duration = 5 })
